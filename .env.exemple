# ---------------------------------
# CONFIGURAÇÃO DE PORTAS (HOST)
# ---------------------------------
# Defina as portas que os serviços usarão no seu 'localhost'.
# Altere aqui se alguma dessas portas já estiver em uso na sua máquina.

# Porta para o PostgreSQL (Banco de Dados)
POSTGRES_PORT=5432

# Porta para o N8N (Automação)
N8N_PORT=5678

# Porta para o Appsmith (HTTP)
# (Usamos 8081 por padrão para evitar conflitos com a porta 80 ou 8080)
APPSMITH_PORT=8081

# Porta para o Appsmith (HTTPS/SSL)
APPSMITH_SSL_PORT=9443

# Porta para o WAHA (WhatsApp API)
WAHA_PORT=3000

# Porta para o Portainer (Gerenciamento Docker)
PORTAINER_PORT=9000

# ---------------------------------
# SEGREDOS E SENHAS
# ---------------------------------

# --- Segredos do Supabase & PostgreSQL ---
# Senha principal do superusuário do Postgres
POSTGRES_PASSWORD=coloque_uma_senha_forte_aqui
POSTGRES_USER=postgres
POSTGRES_DB=postgres

# --- Segredos do N8N ---
# Senha para o banco de dados do N8N (diferente da principal)
N8N_DB_USER=n8n
N8N_DB_PASSWORD=coloque_outra_senha_forte_aqui
N8N_DB_NAME=n8n

# --- Segredos do Appsmith (se for usar) ---
APPSMITH_DB_USER=appsmith
APPSMITH_DB_PASSWORD=coloque_outra_senha_forte_aqui
APPSMITH_DB_NAME=appsmith

# --- Segredos do WAHA ---
WAHA_SWAGGER_USER=admin
WAHA_SWAGGER_PASSWORD=coloque_outra_senha_forte_aqui
